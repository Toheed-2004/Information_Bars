# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.11

# Visualisation (used by compare_bars.py)
matplotlib>=3.7
plotly>=5.15
kaleido>=0.2.1          # static image export for plotly

# Utilities
python-dateutil>=2.8
pyyaml>=6.0
tqdm>=4.65              # progress bars for large tick files

# Optional — only needed if running the ML pipeline (ml/ directory)
# scikit-learn>=1.3
# lightgbm>=4.0
# xgboost>=2.0
